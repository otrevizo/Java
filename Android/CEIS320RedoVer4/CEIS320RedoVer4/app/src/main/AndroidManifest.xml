<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ceis320redover4">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CEIS320RedoVer4">
        <activity android:name=".TakePictureActivity"></activity>
        <activity android:name=".SongActivity"   android:screenOrientation="landscape" />
        <activity android:name=".InformationResultsActivity" />
        <activity android:name=".InformationPageActivity" />
        <activity android:name=".TicTacToeActivity" />
        <activity android:name=".MyDrawing" />
        <activity android:name=".MainMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WeatherActivity" />
    </application>

</manifest>